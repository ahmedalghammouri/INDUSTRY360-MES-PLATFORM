import { SettingsView } from '@/features/settings/settings-view';

export const metadata = { title: 'Settings | INDUSTRY360 MES' };

export default function SettingsPage() {
  return <SettingsView />;
}
