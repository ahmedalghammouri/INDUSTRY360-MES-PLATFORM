import { TraceabilityView } from '@/features/traceability/traceability-view';

export const metadata = { title: 'Traceability | INDUSTRY360 MES' };

export default function TraceabilityPage() {
  return <TraceabilityView />;
}
