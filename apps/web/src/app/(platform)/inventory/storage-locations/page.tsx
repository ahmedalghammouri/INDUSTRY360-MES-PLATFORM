import { StorageLocationsView } from '@/features/inventory/storage-locations-view';

export const metadata = { title: 'Storage Locations | STAR-MES' };

export default function StorageLocationsPage() {
  return <StorageLocationsView />;
}
